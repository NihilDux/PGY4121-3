import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cl.duocuc.app',
  appName: 'PGY4121-2',
  webDir: 'www',
  server: {
    androidScheme: 'http'
  }
};

export default config;
